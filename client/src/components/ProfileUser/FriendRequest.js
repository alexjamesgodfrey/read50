import Popover from 'react-bootstrap/Popover';

const FriendRequest = (props) => {
        // <Popover.Content>
        // {props.username} {props.time} ago
        // </Popover.Content>
        <div>test
        </div>
}

export default FriendRequest;